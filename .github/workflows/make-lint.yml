name: make-lint
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules

    - name: make-lint
      run: npx eslint . --ext .js,.jsx,.ts,.tsx

      