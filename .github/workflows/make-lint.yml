name: make-lint
on: push
jobs:
  build: ver.1
    runs-on: ubuntu-latest 
    steps:
       - uses: actions/checkout@v2
       - name: npm install
         run: | 
           make install
       - name: make-lint
         run: | 
           make lint